# Phase 8.3-A Evidence

This directory contains compact raw evidence from the clean Jetson TensorRT
10.3 RMSNorm `IPluginV3` run. Exit codes, stdout, stderr, environment, build
logs, and the source-hash manifest are preserved. Binaries, engines, core dumps,
and large profiler outputs are intentionally excluded.
